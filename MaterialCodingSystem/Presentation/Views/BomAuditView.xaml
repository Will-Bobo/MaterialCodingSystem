<UserControl x:Class="MaterialCodingSystem.Presentation.Views.BomAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#F3F3F3" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="选择BOM文件…" Padding="12,6" Command="{Binding PickFileCommand}"/>
                <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"
                           Text="{Binding FilePath, FallbackValue=（未选择文件）}" TextTrimming="CharacterEllipsis"/>

                <Button Grid.Column="2" Margin="10,0,0,0" Content="解析/审核" Padding="12,6" Command="{Binding AnalyzeCommand}"/>
                <Button Grid.Column="3" Margin="10,0,0,0" Content="一键导入新物料" Padding="12,6" Command="{Binding ImportAllNewCommand}"/>
                <Button Grid.Column="4" Margin="10,0,0,0" Content="保存审核通过BOM" Padding="12,6"
                        Command="{Binding ArchiveCommand}"
                        IsEnabled="{Binding CanArchive}"
                        ToolTip="{Binding CannotArchiveReason}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock FontWeight="SemiBold" Text="成品编码：" />
                    <TextBlock Margin="6,0,0,0" Text="{Binding FinishedCode}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock FontWeight="SemiBold" Text="版本：" />
                    <TextBlock Margin="6,0,0,0" Text="{Binding Version}" />
                </StackPanel>

                <TextBlock Grid.Column="2" Text="{Binding Total, StringFormat=总数 {0}}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="{Binding Pass, StringFormat=PASS {0}}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="4" Text="{Binding New, StringFormat=新物料 {0}}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="5" Text="{Binding Error, StringFormat=异常物料 {0}}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="6" Text="{Binding MissingCodeError, StringFormat=缺编码 {0}}" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Foreground="DimGray" Text="{Binding Message}" Margin="2,0,0,2"/>
                <TextBlock Foreground="DimGray" Text="{Binding ImportSummary}" Margin="2,0,0,8"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Rows}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      RowHeaderWidth="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="状态" Width="90"
                                        Binding="{Binding StatusRaw, Converter={StaticResource BomAuditStatusToText}}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="{Binding StatusRaw, Converter={StaticResource BomAuditStatusToBrush}}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="编码" Binding="{Binding Code}" Width="180"/>
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="120"/>
                    <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="规格" Binding="{Binding Spec}" Width="200"/>
                    <DataGridTextColumn Header="品牌" Binding="{Binding Brand}" Width="120"/>
                    <DataGridTextColumn Header="异常原因" Binding="{Binding ErrorReason}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#DC2626"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="操作" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="导入物料库"
                                        Padding="10,2"
                                        Visibility="{Binding ShowImportButton, Converter={StaticResource BoolToVis}}"
                                        Command="{Binding DataContext.ImportRowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>

