<UserControl x:Class="MaterialCodingSystem.Presentation.Views.BomArchiveHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#F3F3F3" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Text="成品编码筛选：" />
                <TextBox Grid.Column="1" Margin="10,0,0,0" Text="{Binding FinishedCodeFilter, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Margin="10,0,0,0" Padding="12,6" Content="查询" Command="{Binding RefreshCommand}"/>
                <TextBlock Grid.Column="3" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="DimGray" Text="{Binding Message}"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1" Foreground="DimGray" Text="提示：点击“打开”将以只读方式打开归档文件。" Margin="2,0,0,8"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  RowHeaderWidth="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="finished_code" Binding="{Binding FinishedCode}" Width="160"/>
                <DataGridTextColumn Header="version" Binding="{Binding Version}" Width="220"/>
                <DataGridTextColumn Header="file_path" Binding="{Binding FilePath}" Width="*"/>
                <DataGridTextColumn Header="created_at" Binding="{Binding CreatedAt}" Width="160"/>
                <DataGridTemplateColumn Header="操作" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="打开"
                                    Padding="10,2"
                                    Command="{Binding DataContext.OpenPathCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

