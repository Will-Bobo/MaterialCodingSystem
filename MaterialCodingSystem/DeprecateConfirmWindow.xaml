<Window x:Class="MaterialCodingSystem.DeprecateConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认废弃"
        Width="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Text="确定要废弃该物料吗？"/>

        <Border Grid.Row="1" Margin="0,12,0,0"
                BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8"
                Background="#FAFAFA" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Foreground="#6B7280" Text="编码"/>
                <TextBlock Grid.Row="0" Grid.Column="1" FontWeight="Bold" Text="{Binding Code}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,0,0" Foreground="#6B7280" Text="规格号"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" TextWrapping="Wrap" Text="{Binding Spec}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,0,0" Foreground="#6B7280" Text="规格描述"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,8,0,0" TextWrapping="Wrap" Text="{Binding Description}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,8,0,0" Foreground="#6B7280" Text="名称"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,8,0,0" TextWrapping="Wrap" Text="{Binding Name}"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,8,0,0" Foreground="#6B7280" Text="品牌"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Margin="0,8,0,0" TextWrapping="Wrap"
                           Text="{Binding Brand, TargetNullValue=—}"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,14,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="96" Margin="0,0,8,0" Content="取消" IsCancel="True"/>
            <Button Width="120" Content="确认废弃" IsDefault="True" Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>

