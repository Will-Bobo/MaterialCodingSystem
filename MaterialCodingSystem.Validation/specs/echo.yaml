spec_version: v1

meta:
  system: validation-runner

cases:
  - id: ECHO_001
    title: echo returns input.value as result
    type: core

    given:
      db: {}
      input:
        value: 1

    when:
      action: echo

    then:
      output:
        result: 1

    replay: true
    deterministic: true
